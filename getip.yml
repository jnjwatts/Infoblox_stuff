---
- hosts: localhost
  connection: local
  gather_facts: False

  tasks:
    - name: GetIP
      infoblox:
        server: 13.73.119.145
        username: jwatts
        password: Jeffowesme
        api_version: 2.6
        action: reserve_next_available_ip
        network: "{{the_network}}"
        extattrs: "{{the_host}}"
        comment: "{{the_comment}}"
      register: result

    - name: "Job result"
      debug: msg=" {{result}} "
